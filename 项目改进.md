# 项目改进记录

## 背景

本轮改动主要围绕子 Agent 回传主会话的稳定性与前端展示一致性展开，目标是解决以下两类核心问题：

- 子 Agent 的结果已经产生，但主聊天区经常没有及时显示，或者显示不稳定。
- 多个子 Agent 并发完成时，主 Agent 的后续回复没有稳定融合前一个子 Agent 的结果。

同时，在排查过程中也顺带发现并定位了一个前端 `Failed to fetch` 问题。

---

## 一、已定位并处理的问题

### 1. 主聊天刷新过早，导致子 Agent 结果“看不到”

#### 现象

- 子 Agent 已经完成，Inspector 或子 Agent 卡片里能看到结果。
- 但主聊天区没有及时出现汇总回复。
- 有时工具调用结果像“没有反应”。

#### 根因

前端原本在 `subagent_done / subagent_error / subagent_killed` 时就触发 `loadMainSession()`。

这会导致：

- 子 Agent 虽然已经完成；
- 但其 announce 结果还没有真正写回主会话；
- 前端过早刷新主聊天，把尚未完成写回的内容覆盖掉。

#### 修复

前端刷新主聊天的触发条件改为：

- 仅在 `subagent_announce` 且 `announce_state === delivered / dropped` 时才回拉主会话。

这样主聊天刷新与“announce 真正完成”对齐。

---

### 2. 多个子 Agent 并发完成时，主 Agent 回复不融合

#### 现象

- 两个子 Agent 都完成并回传了结果；
- 主 Agent 也各自回复了；
- 但第二次回复经常看不到第一次子 Agent 的结果，像是各自独立回复。

#### 根因

原来的 announce 回传逻辑只是检查主会话是否 busy，而不是让 announce 真正进入主会话的串行队列。

结果是：

- 两个子 Agent 几乎同时完成时；
- 都可能在主会话未忙的瞬间同时进入 `agent_manager.astream(...)`；
- 两边读取到的是同一份旧上下文；
- 因此彼此看不到对方结果。

#### 修复

主会话 announce 改为：

- 获取当前主会话的 `message_queue`；
- 真正 `acquire()` 队列锁；
- announce 处理结束后再 `release()`。

这样第二个子 Agent 的 announce 必须等第一个 announce 完整写回主会话后才会开始，从而让后一个 announce 能读到前一个结果。

---

### 3. 子 Agent 结果已显示，但 UI 长时间转圈

#### 现象

- 子 Agent 卡片已经出现结果摘要；
- 但界面依然显示像“还在运行”；
- 用户难以判断究竟是子 Agent 没完成，还是主会话还在融合。

#### 根因

系统原先没有把子 Agent 生命周期拆成两个阶段：

1. 子 Agent 自己执行完成
2. 子 Agent 结果回传主会话并被主 Agent 融合

因此前端只能粗略按 `running` 判断状态，无法区分：

- 子 Agent 真在执行
- 子 Agent 已完成，但主会话还在融合

#### 修复

补充 announce 生命周期状态：

- `pending`
- `queued`
- `delivering`
- `delivered`
- `dropped`

并在前端把状态展示拆成：

- 执行中
- 等待主会话融合
- 已完成融合
- 融合失败

这样 UI 不再把“等待融合”误显示成“还在执行”。

---

## 二、前端改动总结

### 1. `frontend/src/lib/hooks/useSubagents.ts`

#### 调整点

- 原来在 `subagent_done / subagent_error / subagent_killed` 时刷新主聊天。
- 现在只在 `subagent_announce: delivered / dropped` 时刷新主聊天。

#### 作用

- 避免过早刷新主会话；
- 避免把子 Agent 结果还没落盘时的旧历史重新拉回来；
- 主聊天展示更稳定。

---

### 2. `frontend/src/lib/api.ts`

#### 调整点

补充 `announce_state` 类型：

- `queued`
- `delivering`

#### 作用

让前端可以消费更完整的 announce 生命周期，而不只是终态。

---

### 3. `frontend/src/components/chat/SubagentInlineCard.tsx`

#### 调整点

将子 Agent 展示态拆为：

- 子 Agent 执行中
- 子 Agent 已完成，等待主会话融合
- announce 已完成
- announce 失败

#### 作用

- 避免用户误以为结果没出来；
- 提升状态可解释性；
- 为后续排障提供更清晰的界面反馈。

---

### 4. `frontend/src/components/inspector/SubagentPanel.tsx`

#### 调整点

细化 trace 文案：

- `执行完成，等待结果合并`
- `等待主会话队列`
- `主Agent正在融合结果`
- `已合并进主会话`
- `结果合并失败，已写入兜底消息`

#### 作用

Inspector 可以更准确反映子 Agent 当前阶段，而不只是显示笼统的“完成/失败”。

---

## 三、后端改动总结

### 1. `backend/graph/subagent_registry.py`

#### 调整点

- 在 `mark_completed()` 时将 `announce_state` 重置为 `pending`
- 新增 `set_announce_state(run_id, state)`

#### 作用

让子 Agent “执行完成”和“announce 回传完成”成为两个独立状态阶段。

---

### 2. `backend/tools/agent_tools.py`

#### 调整点

- 主会话 announce 改为真正进入 `message_queue` 串行执行
- announce 过程加入中间状态：
  - `queued`
  - `delivering`
- 补充两个超时控制：
  - `ANNOUNCE_ACQUIRE_TIMEOUT_SEC`
  - `ANNOUNCE_RUN_TIMEOUT_SEC`
- announce 持锁后显式设置 `queue.set_active_task(asyncio.current_task())`
- 去掉 announce 成功后重复发出的第二次 `subagent_done`

#### 作用

- 解决多个子 Agent 并发完成时的主会话竞态问题；
- 防止第一个 announce 拿到锁后卡住，导致后续 announce 全堵；
- 让 `subagent_done` 只表示“子 Agent 自己完成”，不再和“主会话已融合”混淆。

---

## 四、前后端协同后的新语义

### 1. `state`

仅表示子 Agent 自己的执行状态：

- `running`
- `succeeded`
- `failed`
- `timed_out`
- `cancelled`
- `interrupted`
- `orphaned`

### 2. `announce_state`

仅表示子 Agent 结果回传主会话的阶段：

- `pending`
- `queued`
- `delivering`
- `delivered`
- `dropped`

### 3. 事件语义

- `subagent_done`：子 Agent 自己执行结束
- `subagent_announce: delivered`：主会话已融合完成
- `subagent_announce: dropped`：主会话融合失败，已写入兜底内容

---

## 五、发现但未纳入本轮修复的问题

### 前端 `Failed to fetch`

#### 现象

前端页面加载时在 `fetchAgents()` 报错：

- `TypeError: Failed to fetch`

#### 高概率原因

前端 API 地址按 `window.location.hostname` 拼接：

- 本地访问时是 `localhost:8002`
- 局域网访问时则会变成 `192.168.x.x:8002`

而后端当前默认 CORS 白名单仅包含：

- `localhost`
- `127.0.0.1`

因此当通过局域网地址访问前端时，浏览器可能会因 CORS 拦截而报 `Failed to fetch`。

#### 后续建议

- 开发时优先使用 `http://localhost:3000`
- 或补充后端 `CLAWCHAIN_CORS_ORIGINS`
- 或将局域网访问地址加入允许列表

---

## 六、当前方案的优点与代价

### 优点

- 保留了现有子 Agent 架构，不需要大规模重构；
- 解决了当前最明显的结果回传与状态展示问题；
- 前后端状态语义更清晰；
- 便于继续排查后续问题。

### 代价

- 当前方案仍然是“子 Agent 完成后，以 announce 再触发主会话融合”；
- 这套机制比 Claude Code 那种“子 Agent 结果直接作为当前父回合 tool result”更复杂；
- 状态、锁、超时、UI 协调成本更高。

---

## 七、与 Claude Code 子 Agent 的简单对比

### Claude Code 的特点

- 子 Agent 运行在独立上下文中；
- 中间工具调用和过程不自动合并到主对话；
- 主 Agent 主要收到子 Agent 的 final result；
- 更像一个 delegation primitive（委派工具）。

### 当前项目 / OpenClaw 风格的特点

- 子 Agent 拥有独立 session、可持久化 transcript、可恢复、可归档；
- 支持 `spawn / list / kill / steer` 等运行时控制；
- 子 Agent 结果会通过 announce 链进入父会话 / 主会话；
- 更像一个可调度、可观测、可编排的多 Agent runtime。

### 简单对比结论

Claude Code 的子 Agent 更偏：

- 简洁
- 稳定
- 上下文隔离
- 工具式委派

当前项目这套子 Agent 更偏：

- 长期运行
- 多 Agent 编排
- 可观测
- 可恢复
- 可持久化

因此它的优势在于工程能力更强，但代价是运行时一致性和状态管理更复杂。

---

## 八、后续建议

### 短期建议

- 继续观察 announce 生命周期是否仍存在极端卡住情况
- 若仍有长时间卡住，增加更明确的日志与超时原因上报
- 补充 CORS/网络访问配置，避免前端 `Failed to fetch`

### 中期建议

- 将 announce 状态进一步固化为稳定状态机
- 为子 Agent / announce 加入更完整的运行指标与日志
- 将 UI 中的状态文案国际化，避免硬编码


