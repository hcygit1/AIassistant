# 问题跟踪

## Q1: 当前子 agent 会话用独立 JSON 存，替换为 SQLite 后怎么存

当前每个子 agent 在 `sessions/` 目录下有一个独立的 `subagent-xxx.json` 文件，里面存完整的消息列表。

换成 SQLite 后，建议用以下两张表替代这个 JSON 文件：

- `sessions`：一条记录代表一段会话，字段包括 `session_id`、`agent_id`、`session_type`（`main` / `subagent`）、`parent_session_id`（指向派生它的父会话）、`spawned_by_run_id`、`status`（`active` / `archived`）等。
- `session_messages`：一条记录代表一条消息，字段包括 `session_id`、`agent_id`、`turn_id`、`role`、`content`、`tool_calls_json`、`created_at`、`sequence_no` 等。

子 agent 的消息像主 agent 一样写入 `session_messages`，通过 `session_id` 区分主/子会话，通过 `parent_session_id` 保留父子关系。原来的 JSON 文件会在迁移完成后降级为归档层，不再是主存。

---

## Q2: 子 agent 和父 agent 的会话内容如何写入记忆系统

**确定方案：子 agent 返回主 agent 后，由主 agent 的 `turn_end` 统一触发记忆存储，不单独处理子 agent 会话。**

流程如下：

1. 子 agent 执行完成，把结果汇总成一条 announce 消息投递给主 session（以 `system` 角色写入）。
2. 主 agent 基于 announce 跑一轮，输出 assistant 回应，主对话里此时已包含子 agent 的关键信息汇总版本。
3. 主 agent 本轮结束时触发 `turn_end`，进入记忆处理管线。
4. 取本轮主 session 的 `user / system / assistant` 消息作为候选，做重要性筛选 + embedding，写入 `memory_chunks`。

注意事项：

- `system` 消息不要无条件跳过，announce 类消息可能含有关键任务结果（命令输出、错误修复、关键结论等），需要参与重要性判断。
- 子 agent 原始 session 的完整消息不需要拉进来，主对话里的汇总版本已经足够。
- 子 agent 的原始 session 仍然单独落库（会话事实层），可用于调试和回溯，但不参与主记忆管线。

---

## TODO: 添加 memory_timeline 工具

**背景：** 当前 `get_memory_tools` 只注册了 `memory_search` 和 `memory_get` 两个工具。在自动召回注入的记忆片段中，agent 只能用 `memory_get(chunk_id)` 查看单条完整内容，缺少一个能"展开上下文"的工具——即给定一个 chunk_id，返回它前后 N 条同 session 的消息，帮助 agent 理解记忆片段的完整对话上下文。

**预期功能：**

```
memory_timeline(chunk_id, window=5)
```

- 输入：一个 chunk_id（从召回结果或 memory_search 获得），以及可选的窗口大小
- 行为：找到该 chunk 所属的 session_key，按 created_at 排序取前后 window 条 chunks
- 输出：按时间顺序返回这些 chunks 的 role、summary/content_excerpt、时间戳，标记出原始 chunk 的位置

**实现位置：**
- 工具定义：`backend/tools/memory_tools.py` — 新增 `MemTimelineTool` 类
- 存储层查询：`backend/mem/store.py` — 新增 `get_chunk_timeline(chunk_id, window)` 方法
- 注册：`get_memory_tools()` 返回列表中加入 `MemTimelineTool`
- `_format_recall_as_prompt` 中的提示文字可恢复 timeline 引用

**依赖：** chunks 表已有 `session_key` 和 `created_at` 字段，无需加列。

检索到的task和chunk进行格式化拼接还没有实现？
将前半部分截断总结怎么判断在哪里截断，百分比是一方面，如何避免一个完整的对话被切断呢，比如工具调用结果被切为两半这种情况？
压缩后的结构化内容控制在多少token？
重点对工具的结果做特殊处理吗，类似信息也是一样的问题？
保留最近的关键交互、架构决策、未解决 bug 等高价值信息，这种是如何判断的？
通常上下文中各部分召回的内容控制在多少大小，要预留多少会话窗口大小，超过多少阈值大模型能力开始下降？

上下文中各部分的典型占比（推荐实践）
Anthropic 没有给出严格的固定数字，但从 Claude Code 的实现和博客案例中可以提炼出以下实用分配建议（以 200k 上下文窗口为例，目标是保持活跃上下文在 60-70% 以内，即约 120k-140k tokens）：

系统 Prompt .md（用户画像，agent画像，行为习惯等）：2,000 – 5,000 tokens（固定 upfront 加载，约 3-5%）。
持久化结构化文件：1,000 – 4,000 tokens（记忆读取或启动时注入，约 2-5%）。
最近几轮高价值交互：15,000 – 30,000 tokens（保留最近 8-15 轮原始消息，约 10-20%）。
压缩后的结构化总结：1,500 – 3,500 tokens（有损总结等）。
当前工具调用结果 / JIT 数据：动态控制，单次 tool output 建议限制在 10k-25k tokens。
总活跃上下文推荐上限：100k – 140k tokens（60-70%），预留 30-40% 作为“思考预算”（thinking budget）。

关键原则：上下文不是越多越好。超过 50% 占用时模型能力就开始渐进下降（context rot），70-80% 以上推理明显变弱。优先通过外部文件和 JIT 工具减少对上下文窗口的依赖。

当前的purning是如何做的？
要统一的context长度管理组件
先调整第一层规划，确定各层阈值，然后再进行下面的分析


阶段一：规划定义
"需要什么上下文、预算怎么分"

你的实现：

定义项	在哪配置	当前值
上下文窗口总量
config.agents.defaults.contextTokens
200,000
System prompt 模式
PromptParams.mode
full / minimal / none
文件注入上限
MAX_FILE_CHARS / MAX_TOTAL_CHARS
20K / 80K chars
压缩阈值
compaction.slidingThreshold / forcedThreshold
0.75 / 0.90
保留轮次
compaction.keepRecentTurns
8 轮
修剪策略
contextPruning.*
toolOutputMaxChars=3000, recentPreserve=4
记忆召回预算
mem.recall.budget_chars / skill_budget_chars
4000 / 2000 chars
现状评价：各层预算有各自的配置项，但没有一个全局的预算分配模型把它们关联起来。每个模块各自定义上限，互不感知当前上下文实际用了多少。

阶段二：收集选择
"从哪些数据源取、怎么筛选"

你的实现涉及 5 个数据源：

数据源 ①: 静态指令片段（prompt_builder 硬编码的 15+ 个 section）
           ↓ _build_identity / _build_tooling / _build_safety / ...
数据源 ②: 项目文件（磁盘读取 → SOUL.md / USER.md / IDENTITY.md / AGENTS.md / TOOLS.md）
           ↓ _build_project_context_with_report()
数据源 ③: 会话历史（JSON 文件 → session_manager.load_session_for_agent）
           ↓ 含 compressed_context（旧版字段）+ messages
数据源 ④: 压缩摘要（MemStore → store.get_session_summary）
           ↓ 压缩后生成的结构化 JSON
数据源 ⑤: 长期记忆（MemStore → recall.search，被动，仅当 agent 调用 memory_search 工具时）
           ↓ Tasks / orphan Chunks / Skills
选择/筛选机制：

项目文件：_smart_truncate 按 70% 头 + 20% 尾截断；总量超 MAX_TOTAL_CHARS 时后面的文件被跳过
会话历史：load_session_for_agent 合并连续 assistant 消息（减少消息条数）
记忆召回：MemRecall 用 FTS5 + ANN → RRF 融合 → recency decay 排序，budget_chars 控制总量
记忆入库时：MemWorker 做 hash 精确去重 + LLM 语义去重（DUPLICATE / UPDATE / NEW 三分类）
现状评价：收集源丰富，筛选有分层。但数据源 ⑤（长期记忆）完全依赖 agent 主动调用，系统不自动收集。

阶段三：组装注入
"按什么顺序、以什么格式拼成最终的 messages"

这是 agent.py astream() 方法的核心流程，按执行顺序：

步骤 1: prompt_builder.build_system_prompt_with_report(params)
        → 组装 system_prompt（指令 + 项目文件，作为 create_react_agent 的 prompt 参数）
步骤 2: session_manager.load_session_for_agent(session_id, agent_id)
        → 加载会话历史 messages[]
        → 如果有旧版 compressed_context，注入到 history 头部（assistant 角色）
步骤 3: store.get_session_summary(session_id, agent_id)
        → 如果有压缩摘要，format_session_summary() 格式化后 insert 到 history[0]（system 角色）
步骤 4: prune_messages(history)
        → 修剪旧工具输出（>3K chars 截断到 500 chars）
        → 修剪大型 system 消息（>2K chars 截断）
步骤 5: _build_messages(history, new_message)
        → 转为 LangChain 消息对象（HumanMessage / AIMessage / SystemMessage）
        → system 消息如果不在首位则降级为 HumanMessage
步骤 6: create_react_agent(model=llm, tools=tools, prompt=system_prompt)
        → agent.astream_events({"messages": lc_messages})
最终送入模型的上下文结构：

┌─ system prompt ─────────────────────────────────────────────┐
│  identity + tooling + safety + skills + memory指令 + ...    │
│  + SOUL.md + USER.md + IDENTITY.md + ...                    │
└─────────────────────────────────────────────────────────────┘
┌─ messages[] ────────────────────────────────────────────────┐
│  [0] session_summary（压缩摘要，仅压缩后存在）               │
│  [1] compressed_context（旧版，逐步废弃）                    │
│  [2..N-1] 保留的近期消息（已修剪工具输出）                    │
│  [N] 当前用户消息                                           │
└─────────────────────────────────────────────────────────────┘
现状评价：组装流程清晰，但有两个冗余——compressed_context（旧版纯文本摘要）和 session_summary（新版结构化摘要）同时可能存在，注入了两份压缩上下文。

阶段四：压缩维护
"随着对话变长，怎么控制上下文不爆"

你有 4 层压缩/维护机制，按触发时机从早到晚：

层	机制	触发时机	实现位置
L1 修剪
旧工具输出截断
每轮送入 LLM 前
session_pruning.prune_messages()
L2 滑动压缩
LLM 生成结构化摘要 + 删旧消息
_maybe_auto_compact 检测到 token > 75%
agent.compress_session(level="sliding")
L3 强制压缩
同上，但保留更少轮次
token > 90%
compress_session(level="forced")
L4 会话重置
归档整个会话文件
用户 /new 或压缩失败时
_handle_reset / _handle_reset_noflush
压缩的同时做记忆入库：

每轮结束 → _incremental_ingest() → MemWorker
    → hash 去重 → LLM summary → embedding → 语义去重 → INSERT chunk
压缩时 → _batch_ingest_messages() → MemWorker（批量入库被压缩掉的消息）
/new 重置时 → _batch_ingest_messages(session_end=True)
    → MemTaskProcessor.on_chunks_ingested(session_end=True)
    → finalize 当前 Task（LLM 生成任务摘要 + embedding）
    → MemSkillEvolver.on_task_completed（评估是否产出 Skill）
现状评价：压缩管线完整，从 L1 到 L4 逐级升级。记忆入库和压缩联动。主要问题是 L1（修剪）太简单，L2/L3 阈值偏高导致 context rot 已经发生才压缩。

阶段五：评估迭代
"怎么知道上下文管理做得好不好"

你当前有的评估手段：

评估手段	实现	评估了什么
PromptReport
prompt_builder.build_system_prompt_with_report()
system prompt 的字符数、section 数、截断次数、工具数
RunTracker
run_tracker.start_turn / complete_turn
每轮 input/output tokens、耗时、工具调用次数
AuditLogger
audit_logger.log_*
turn 开始/结束/错误、工具调用、压缩事件
AgentState
state.record_turn()
累计 input/output tokens、总轮次、压缩次数
现状评价：这一层是最薄的。你有运行指标的记录，但缺少上下文质量的评估：

没有追踪"上下文实际使用率"（各层各占了多少比例）
没有追踪"记忆召回命中率"（agent 调用 memory_search 后是否真的用了结果）
没有追踪"压缩信息损失"（压缩后 agent 是否还能回答关于旧对话的问题）
没有 A/B 对比不同阈值/预算设置的效果
总结：五阶段完整度一览
规划定义   ██████░░░░  60%  — 各层有独立配置，但缺全局预算协调
收集选择   ████████░░  80%  — 多数据源 + 去重 + 筛选，但长期记忆只有被动工具
组装注入   ████████░░  80%  — 流程清晰，有 compressed_context/session_summary 冗余
压缩维护   █████████░  90%  — 四级压缩 + 记忆入库联动，阈值偏高是主要问题
评估迭代   ███░░░░░░░  30%  — 有运行指标记录，缺上下文质量评估
你的项目在压缩维护这一环做得最扎实（四级机制 + 记忆管线），评估迭代最薄弱。如果要按优先级改进，建议是：先调阈值（规划定义层，改 config 立刻见效）→ 再清理冗余（组装注入层，去掉旧版 compressed_context）→ 最后补评估（加上下文使用率 dashboard）。

总窗口: contextTokens (默认 200K)

一级切分:
  thinkingReserve  = 0.30    → 60K tokens 留给模型推理
  activeBudget     = 0.70    → 140K tokens 可用于注入

二级切分 (activeBudget 内部):
  systemPrompt     = 0.05    
  sessionSummary   = 0.03   
  recentMessages   = 0.82    
  jitToolOutput    = 0.10   

压缩触发点 (基于 activeBudget):
  slidingThreshold = activeBudget × 0.77  ≈ 100K tokens (总窗口的 50%)
  forcedThreshold  = activeBudget × 1.0   = 140K tokens (总窗口的 65%)

on_tool_end → 截取输出（output_str[:2000]），写审计日志，emit tool_end 事件
recursion_limit是什么
JIT 截断是什么