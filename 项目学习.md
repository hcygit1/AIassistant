# 模块一：配置系统 `config.py`

> 学习路径：从地基往上，配置是所有模块的原材料来源

## 一、整体定位

配置系统是整个项目的**原材料供应商**。所有模块（agent、session、heartbeat、tools、memory...）需要参数时都来这里取，自己不保存配置状态。

---

## 二、数据流图

```
文件系统                    内存                      其他模块
──────────────────────────────────────────────────────────────
config.template.json ──┐
                        ├─→ load_config()
data/config.json ───────┘       │
                                │ _deep_merge(DEFAULT_CONFIG, saved)
                                ↓
                        _raw_config (原始值，含 ${ENV_VAR})
                                │
                                │ _resolve_env_vars()
                                ↓
                        _config (运行时值，已展开环境变量)
                                │
                        ┌───────┴───────────────────────┐
                        ↓                               ↓
                  get_config()                  _init_models_config()
                  供所有模块读取                初始化模型配置管理器
```

---

## 三、关键设计

### 1. 两份拷贝：`_raw_config` vs `_config`

**声明位置**（`config.py` 第 191-193 行）：

```python
_config: dict[str, Any] | None = None
_raw_config: dict[str, Any] | None = None
```

**赋值流程**（`load_config()` 内）：

1. 从磁盘读 `config.json` → 合并默认值 → 存入 `_raw_config`
2. 对 `_raw_config` 做 `copy.deepcopy` + `_resolve_env_vars()` → 存入 `_config`

**用途区分**：

| 变量 | 内容 | 用途 |
|------|------|------|
| `_raw_config` | 原始配置，含 `${VAR}` 占位符 | 写回磁盘时用，保留占位符不泄露密钥 |
| `_config` | 展开后的配置，含真实密钥 | 运行时给各模块用 |

**为什么要两份？**

- **写回磁盘**：必须保留 `${OPENAI_API_KEY}` 原样，不能把真实 key 写进文件（避免泄露、git 追踪）
- **运行时使用**：LLM 创建需要真实 `api_key`，不能是 `${VAR}` 字符串
- **不可逆**：展开后无法从 `sk-proj-xxxxx` 还原回 `${OPENAI_API_KEY}`，所以必须同时维护两份

**正确数据流**：

```
磁盘 config.json（含 "${OPENAI_API_KEY}"）
        │
        ▼ load_config()
_raw_config  ← 存原始值，不展开，保留 "${OPENAI_API_KEY}"
        │
        │ copy.deepcopy + _resolve_env_vars()
        ▼
_config      ← 存展开后的值，"sk-proj-xxxxx"
```

- `_raw_config` 不负责读环境变量，只存原始值
- `_resolve_env_vars()` 负责从 `os.environ` 读真实值并替换
- 写回时：`save_config(cfg)` 的 `cfg` 由调用方传入（通常是 `get_raw_config()` 的原始配置），不是从 `_config` 传回

---

### 2. 深度合并：`_deep_merge`

**合并对象**：`DEFAULT_CONFIG`（代码硬编码默认值）+ 用户 `config.json`

**作用**：用户只需写差异，缺失字段自动从 `DEFAULT_CONFIG` 继承。

**实现**（第 208-216 行）：

```python
def _deep_merge(base: dict, override: dict) -> dict:
    result = base.copy()
    for k, v in override.items():
        if k in result and isinstance(result[k], dict) and isinstance(v, dict):
            result[k] = _deep_merge(result[k], v)   # 递归合并嵌套 dict
        else:
            result[k] = v
    return result
```

**与浅合并的区别**：若用 `{**base, **override}`，用户写 `"heartbeat": {"enabled": true}` 会整块替换，`every`、`ackMaxChars` 等字段丢失。深度合并保证只覆盖用户显式写的字段。

---

### 3. Agent 两层覆盖

**结构**：

```json
{
  "agents": {
    "defaults": { ... },   // 第一层：所有 agent 共享的默认值
    "list": [              // 第二层：每个 agent 的个性化覆盖
      { "id": "main", ... },
      { "id": "coder", ... }
    ]
  }
}
```

**合并逻辑**（`resolve_agent_config()` 第 372-380 行）：

```python
def resolve_agent_config(agent_id: str) -> dict[str, Any]:
    defaults = get_agent_defaults()
    for agent in list_agents():
        if agent["id"] == agent_id:
            merged = {**defaults, **{k: v for k, v in agent.items() if v is not None}}
            return merged
    return defaults
```

**注意**：这里是浅合并，若 agent 只想改 `heartbeat.enabled`，需写完整 `heartbeat` 对象，否则会丢失 `every` 等字段。

---

## 四、路径解析函数族

所有 Agent 数据路径集中定义，全项目统一引用：

| 函数 | 路径 |
|------|------|
| `resolve_agent_dir(agent_id)` | `data/agents/<id>/` |
| `resolve_agent_workspace(agent_id)` | `data/agents/<id>/workspace/` |
| `resolve_agent_memory_dir(agent_id)` | `data/agents/<id>/workspace/memory/` |
| `resolve_agent_sessions_dir(agent_id)` | `data/agents/<id>/sessions/` |
| `resolve_agent_knowledge_dir(agent_id)` | `data/agents/<id>/knowledge/` |
| `resolve_agent_storage_dir(agent_id)` | `data/agents/<id>/storage/` |
| `resolve_agent_skills_dir(agent_id)` | `data/agents/<id>/workspace/skills/` |

---

## 五、写保护与原子写入

```python
def save_config(cfg, *, validate=True):
    if validate:
        result = validate_config(cfg)
        if not result.ok:
            raise ValueError(...)
    tmp = p.with_suffix(".tmp")
    json.dump(cfg, tmp)
    shutil.move(tmp, p)   # 原子替换
```

---

## 六、学习过程中的问题与解答

### Q1: 两份拷贝体现在哪里？

体现在 `load_config()` 中：`_raw_config` 存合并后的原始值；`_config = _resolve_env_vars(copy.deepcopy(_raw_config))` 存展开后的副本。两者是独立的内存对象。

### Q2: 深度合并是合并了什么？

合并 `DEFAULT_CONFIG` 和用户的 `config.json`。用户只写差异，其余从默认值继承。递归处理嵌套 dict，避免整块覆盖导致字段丢失。

### Q3: Agent 两层是指什么？

`agents.defaults` 是全局默认，`agents.list[i]` 是每个 agent 的覆盖。`resolve_agent_config(agent_id)` 把两者合并成该 agent 的完整配置。

### Q4: 为什么要用两份拷贝？

- 写回磁盘：必须保留 `${VAR}`，不能写真实密钥
- 运行时：需要真实值才能调 API
- 展开不可逆，所以必须同时维护原始版和展开版

### Q5: 是 raw_config 负责把环境变量读出来吗？

不是。`_raw_config` 只存原始值。`_resolve_env_vars()` 负责从 `os.environ` 读值并替换，结果写入 `_config`。写回时 `save_config(cfg)` 的 `cfg` 由调用方传入原始配置，不是从 `_config` 反推。

---

## 七、下一步

进入 **模块二：会话管理 `session_manager.py`** —— 理解数据怎么存。

---

# 模块二：会话管理 `session_manager.py`

> 学习路径：从地基往上，会话管理是对话数据的持久化层

## 一、整体定位

会话管理器负责**对话消息的持久化**：每条 user/assistant/system 消息的读写、会话列表、压缩、重置，都由它完成。它是 Agent 和磁盘之间的唯一通道，所有需要读写会话的模块都通过 `session_manager` 单例访问。

**本质**：会话管理 = 主/子 Agent 的对话数据**增删改查**。不负责"调度"（何时跑、跑哪个 session），只负责**存和取**。

---

## 二、存储架构：双文件结构

每个 Agent 的会话数据分两部分：

```
data/agents/<agent_id>/sessions/
├── sessions.json              ← 索引：session_key → { sessionId, updatedAt, label, spawnedBy }
├── main-main.json             ← 主会话的完整消息
├── subagent-abc123.json       ← 子会话的完整消息
├── archive/                   ← 被 prune/reset 的旧会话归档
│   ├── main-main.reset.1234567890.json
│   └── subagent-xxx.deleted.1234567890.json
└── compactions.jsonl          ← 压缩历史（审计用）
```

- **`sessions.json`**：索引，只存 session_key、sessionId、updatedAt、label、spawnedBy，不存消息内容
- **`<session_id>.json`**：单个会话的完整数据，包含 messages 数组

---

## 三、核心概念

### 1. session_id vs session_key

| 概念 | 格式 | 示例 |
|------|------|------|
| `session_id` | 文件命名用 | `main-main`、`subagent-abc123` |
| `session_key` | 索引中的 key，带 agent 前缀 | `agent:main:main`、`agent:main:subagent:subagent-abc123` |

转换函数：
- `session_key_from_session_id(agent_id, session_id)`：主会话 → `agent:id:main`，子会话 → `agent:id:subagent:{session_id}`
- `session_id_from_session_key(session_key)`：解析出 `(agent_id, session_id)`

主会话的 `session_id` 固定为 `{agent_id}-main`，子会话为 `subagent-{uuid}`。

### 2. 单会话 JSON 结构

```python
{
    "session_id": "main-main",
    "agent_id": "main",
    "created_at": 1234567890.0,
    "updated_at": 1234567890.0,
    "compressed_context": "之前的对话摘要...",
    "messages": [
        {"role": "user", "content": "..."},
        {"role": "assistant", "content": "...", "tool_calls": [...]},
        {"role": "system", "content": "..."}   # 子 agent announce 等
    ],
    "label": "可选标题",
    "spawned_by": "agent:main:main"   # 子会话才有
}
```

---

## 四、数据流

### 读路径

```
load_session(session_id, agent_id)
    │
    ├─ 1. 查 LRU 缓存 (agent_id:session_id)
    │     命中 → 直接返回
    ├─ 2. 读磁盘 <session_id>.json
    ├─ 3. 兼容处理（list→dict、bootstrap label 清理）
    └─ 4. 放入缓存，返回
```

`load_session_for_agent` 在此基础上做 LLM 优化（第 162-193 行）：
1. 若有 `compressed_context`，在头部加一条 `assistant` 消息：`[以下是之前对话的摘要]\n{compressed}`
2. 合并连续 `assistant` 消息（LLM 要求 role 交替）

### 写路径

```
save_message / ensure_session / _save_session_data
    │
    ├─ 1. 按 agent_id:session_id 加锁（防止并发写同一会话）
    ├─ 2. 写 <session_id>.json
    ├─ 3. 更新 LRU 缓存
    └─ 4. 更新 sessions.json 索引（updatedAt、label、spawnedBy）
```

---

## 五、关键设计

### 1. 按会话加锁

每个 `agent_id:session_id` 一把 `threading.Lock`，同一会话的并发写串行化。

### 2. LRU 缓存（max_size=30）

`OrderedDict` 实现 LRU，避免频繁读磁盘。

### 3. 压缩机制 `compress_history`

当消息过多触发压缩时：
1. 取前 `n_messages` 条消息，移到 `archive/{session_id}_{ts}.json`
2. 用 LLM 生成的 `summary` 追加到 `compressed_context`（多次压缩用 `---` 分隔）
3. `messages` 只保留剩余部分
4. 在 `compactions.jsonl` 记一条审计日志

### 4. 维护逻辑 `_run_session_maintenance`

- **prune**：`updatedAt` 早于 `pruneAfter`（如 30d）的会话 → 移到 archive
- **cap**：会话数超过 `maxEntries`（如 500）→ 删最旧的
- **disk budget**：`sessions/` 目录超过 `maxDiskBytes` → 先删 archive，再删非主会话

**mode=warn 且 enforce=False 时**：不执行 prune/cap，只做磁盘预算 dry_run，直接返回。

### 5. rollback_last_turn

心跳返回 `HEARTBEAT_OK` 时，撤销最后一轮 user+assistant，避免把"待机正常"当成真实对话存下来。

---

## 六、函数列表

| 函数 | 作用 |
|------|------|
| `load_session` | 读会话（先缓存再磁盘），返回 dict 或 None |
| `load_session_for_agent` | 为 LLM 准备消息列表（含 compressed_context 注入、assistant 合并） |
| `ensure_session` | 存在则返回，不存在则创建 |
| `save_message` | 追加一条消息并持久化 |
| `rollback_last_turn` | 心跳 HEARTBEAT_OK 时撤销最后一轮 |
| `list_sessions` | 列出会话，扫描 subagent-*.json 补全索引 |
| `create_session` | 创建新会话 |
| `rename_session` | 更新 label |
| `delete_session` | 删除会话 |
| `compress_history` | 归档旧消息、追加 summary 到 compressed_context |
| `reset_session` | 归档旧会话 → 创建新会话 |
| `get_active_session_id` | 返回最近活跃的会话 ID |
| `resolve_main_session_id` | 返回 `{agent_id}-main` |
| `session_key_from_session_id` | session_id → session_key |
| `session_id_from_session_key` | session_key → (agent_id, session_id) |
| `derive_session_title` | 从 label/首条消息推导标题 |

---

## 七、父子 Agent 与会话

- 主会话：`main-main`，每个 Agent 一个
- 子会话：`subagent-{uuid}`，由 `sessions_spawn` 创建
- 子会话通过 `spawned_by=requester_key` 与父会话关联
- 子 Agent 在**独立 session** 里跑，有独立队列，不阻塞主会话
- 子 Agent 完成后，announce 以 **system 消息**写入主会话

---

## 八、与记忆系统 MVP 的关系

按 `memory-system-mvp-plan.md`，当前 JSON 会话存储会被 SQLite 的 `sessions` + `session_messages` 替代。改造时需**保持接口签名不变**，内部实现从文件改为数据库。理解现在的双文件结构和读写路径，有助于设计迁移方案。

---

## 九、下一步

进入 **模块三：Agent 核心 `agent.py`** —— 系统心脏，消息如何进入、LLM 如何调用、工具如何执行。

---

# 模块三：Agent 核心 `agent.py`

> 学习路径：系统心脏，消息如何进入、LLM 如何调用、工具如何执行

## 一、整体定位

Agent 核心是系统的**执行引擎**：用户消息进入后，经过命令解析、记忆刷新、工具构建、LLM 调用、工具执行、消息保存、自动压缩等步骤，最终产出流式响应。它是配置、会话、工具、记忆等模块的**编排中心**。

---

## 二、AgentState — 每个 Agent 的运行时状态

**位置**：第 71-148 行

### 参数说明

| 参数 | 类型 | 含义 |
|------|------|------|
| `agent_id` | str | Agent 唯一标识，如 `main`、`coder` |
| `compaction_count` | int | 当前会话已压缩次数；与 `memory_flush_compaction_count` 配合避免重复 flush；`/new`、`/reset`、错误恢复时清零 |
| `total_input_tokens` | int | 累计输入 token，用于用量统计、`/usage` |
| `total_output_tokens` | int | 累计输出 token |
| `total_turns` | int | 累计对话回合数 |
| `think_level` | int | 思考深度（0=关，1+=开），对应 `/think` 命令 |
| `verbose` | bool | 详细输出模式，对应 `/verbose` |
| `reasoning` | bool | 显示推理过程，对应 `/reasoning` |
| `last_active` | float | 最近活动时间戳，`record_turn` 时更新 |
| `_tools_cache` | list \| None | 工具列表缓存，`invalidate_tools` 时清空 |

### 派生属性

- `thinking`：`think_level > 0` 时为 True

### 主要方法

- `record_turn(input_tokens, output_tokens)`：每轮结束调用，累加 token 和 turns
- `invalidate_tools()`：清空工具缓存
- `to_dict()` / `from_dict()`：序列化 / 反序列化
- `save_to_disk(path)` / `load_from_disk(path, agent_id)`：持久化到 `data/agents/<id>/agent_state.json`

---

## 三、AgentManager — 核心引擎

**位置**：第 210-1522 行

### 参数说明

| 参数 | 类型 | 含义 |
|------|------|------|
| `data_dir` | str | 数据根目录，`initialize` 时设置 |
| `memory_indexers` | dict[str, MemoryIndexer] | 每个 Agent 的 RAG 索引器，用于 `retrieve` 和 session-memory 后 `rebuild_index` |
| `memory_search_engines` | dict[str, MemorySearchEngine] | 每个 Agent 的记忆搜索引擎 |
| `_states` | dict[str, AgentState] | 每个 Agent 的状态，`get_state(agent_id)` 获取或创建 |
| `_initialized` | bool | 是否已完成 `initialize` |
| `_main_loop` | asyncio.AbstractEventLoop \| None | 主事件循环；子 Agent 在工具中由同步上下文启动，需 `run_coroutine_threadsafe(coro, _main_loop)` 投递 |
| `lifecycle_hooks` | LifecycleHooks \| None | 工具调用前后钩子（审计、确认），当前未接入，始终为 None |
| `_pending_tasks` | set[asyncio.Task] | 后台任务（如 `/new` 的 session-memory 保存），关闭前 `wait_for_pending_tasks` 等待 |
| `_state_save_tasks` | dict[str, asyncio.Task] | 每个 Agent 的定期状态保存任务，按 `statePersist.autoSaveIntervalMinutes` 间隔写入磁盘 |

---

### 函数列表（按职责分类）

#### 初始化与生命周期

| 函数 | 作用 |
|------|------|
| `initialize(data_dir)` | 为每个 agent 创建 workspace、MemoryIndexer、MemorySearchEngine 并 rebuild_index；加载 AgentState；启动 `_periodic_state_save`；设置 `_main_loop` |
| `wait_for_pending_tasks(timeout)` | 关闭前：保存所有 Agent 状态、取消 `_state_save_tasks`、等待 `_pending_tasks` 完成 |
| `_save_all_states()` | 遍历 `_states`，将每个 AgentState 写入 `agent_state.json` |
| `_periodic_state_save(agent_id)` | 按 `autoSaveIntervalMinutes` 定期保存该 agent 的状态到磁盘 |
| `register_agent(agent_id)` | 动态注册新 agent：创建 workspace、索引、搜索引擎、AgentState |

#### LLM 与模型

| 函数 | 作用 |
|------|------|
| `get_llm(agent_id)` | 通过 `resolve_agent_model` + `llm_cache.get_or_create` 获取该 agent 的 LLM 实例 |
| `get_current_model_ref(agent_id)` | 返回该 agent 当前使用的 ModelRef |
| `switch_model(agent_id, model_raw)` | 解析 model_raw → 使 llm_cache 失效 → 创建新 LLM；返回新模型显示名 |

#### 状态与工具

| 函数 | 作用 |
|------|------|
| `get_state(agent_id)` | 返回 `_states[agent_id]`，不存在则创建并返回 |
| `_build_tools(agent_id, session_id)` | 聚合 file、exec、web、memory、knowledge、agent、cron、status 工具，传入 `_filter_tools_by_policy` 过滤 |
| `_filter_tools_by_policy(agent_id, tools)` | 按 `agents.list[].tools.allow/deny` 过滤；deny 优先；allow 为空表示全允许 |
| `_build_messages(history, new_message)` | 将 dict 格式的 history 转为 LangChain HumanMessage/AIMessage/SystemMessage，末尾追加 new_message |

#### 命令处理

| 函数 | 作用 |
|------|------|
| `_handle_reset(session_id, agent_id, model_override)` | `/new`：后台保存 session-memory → reset_session → 清零 compaction_count；可选切换模型；yield command_response、session_reset；不 yield done，后续用 BARE_SESSION_RESET_PROMPT 跑问候 |
| `_handle_reset_noflush(session_id, agent_id)` | `/reset`：不写记忆，直接 reset_session → 清零 compaction_count；同上不 yield done |
| `_handle_compact(session_id, agent_id)` | `/compact`：调用 `compress_with_flush`，yield 结果或错误详情 |

#### 记忆与压缩

| 函数 | 作用 |
|------|------|
| `run_memory_flush(session_id, agent_id)` | 压缩前记忆刷新：用 LLM 对当前对话做摘要，以 `[记忆刷新] xxx` 的 system 消息追加；无内容则返回 NO_REPLY |
| `save_session_memory(session_id, agent_id)` | 同步保存 session-memory：加载会话 → 调用 `_save_session_memory_from_messages` |
| `_save_session_memory_background(...)` | 后台异步保存 session-memory，用于 `/new` 不阻塞；完成后 event_bus.emit session_memory_saved/failed |
| `_save_session_memory_from_messages(...)` | 用 LLM 生成摘要 → 写入 `memory/{today}-{slug}.md` → 触发 indexer.rebuild_index |
| `compress_with_flush(session_id, agent_id)` | 完整压缩流程：1) 若需则 run_memory_flush；2) 计算可压缩消息数；3) `_summarize_with_fallback` 生成摘要；4) session_manager.compress_history；5) 注入 post_compaction_context；6) compaction_count += 1 |
| `_summarize_with_fallback(...)` | 摘要生成：全量重试 → 排除超大消息部分摘要 → 最终降级为仅记录 |
| `_calc_compress_count(messages, keep_recent_tokens)` | 从尾部向前累加 token，超过 keep_recent_tokens 时停止，返回可压缩条数 |
| `_maybe_auto_compact(session_id, agent_id)` | Turn 结束后：若 `should_compact` 为真则调用 `compress_with_flush`，并 event_bus.emit auto_compact_start/done |

---

### astream 详解 — 主入口与核心流程

**签名**（第 448-455 行）：

```python
async def astream(
    self, message: str, session_id: str, agent_id: str = "main",
    prompt_mode: str = "full",
    persist_input_role: str = "user",
) -> AsyncGenerator[dict[str, Any], None]:
```

**参数**：
- `message`：用户输入或系统注入（如 BARE_SESSION_RESET_PROMPT）
- `session_id`：会话 ID
- `agent_id`：Agent ID
- `prompt_mode`：`full`（主对话）/ `minimal`（子 Agent、announce、心跳）
- `persist_input_role`：保存 message 时的 role，通常 `user`，announce 时为 `system`

---

#### astream 流程总览

```
┌─────────────────────────────────────────────────────────────────┐
│ 1. 命令处理                                                       │
│    parse_command → execute_command                               │
│    /new → _handle_reset → message=BARE_SESSION_RESET_PROMPT      │
│    /reset → _handle_reset_noflush → message=BARE_SESSION_RESET   │
│    /compact → _handle_compact → return                           │
│    /stop、/status 等 → yield done → return                        │
└─────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│ 2. 压缩前 Memory Flush（可选）                                     │
│    should_run_memory_flush → run_memory_flush                     │
│    set_memory_flush_compaction_count 避免重复                      │
└─────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│ 3. 准备输入                                                       │
│    BOOTSTRAP.md 检测 → extra_prompt                               │
│    _build_tools → _filter_tools_by_policy                         │
│    prompt_builder.build_system_prompt_with_report                 │
│    load_session_for_agent → prune_messages                       │
│    RAG（full 模式）：indexer.retrieve → 追加 assistant 消息       │
│    _build_messages(history, message) → lc_messages                │
└─────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│ 4. 模型 Fallback 链                                               │
│    candidates = resolve_fallback_candidates(agent_id)             │
│    run_with_fallback_stream(candidates, run_for_model, ...)       │
└─────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│ 5. run_for_model 内层（单模型执行）                                │
│    create_llm(ref) → create_react_agent(llm, tools, prompt)       │
│    run_tracker.start_turn → audit_logger.log_turn_start          │
│    agent.astream_events(lc_messages, recursion_limit)            │
│      ├─ on_chat_model_stream → yield token, record_tokens        │
│      ├─ on_tool_start → lifecycle_hooks, yield tool_start        │
│      ├─ on_tool_end → audit, yield tool_end, event_bus(危险工具)  │
│    run_tracker.complete_turn → state.record_turn                 │
│    Fallback 工具调用：parse_text_tool_calls → 手动执行工具         │
│    save_message(user) → save_message(assistant, tool_calls)     │
│    yield turn_end, done                                           │
│    _maybe_auto_compact                                            │
└─────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│ 6. 外层异常与重试                                                 │
│    while True: run_with_fallback_stream(...)                     │
│    瞬时 HTTP 错误 → 等待 2.5s 重试一次                            │
│    压缩失败 → reset_session, yield session_reset + done           │
│    role 顺序错误 → reset_session, yield done                      │
│    会话损坏 → reset_session, yield done                          │
│    context 溢出 → 直接 yield error（不换模型）                     │
└─────────────────────────────────────────────────────────────────┘
```

---

#### astream 产出事件类型

| type | 含义 |
|------|------|
| `token` | 流式文本块，`content` 为当前 chunk |
| `tool_start` | 工具开始执行，含 `tool`、`input`、`step`、`max_steps` |
| `tool_end` | 工具执行结束，含 `tool`、`output` |
| `content_refresh` | Fallback 工具调用时，用清理后的 content 刷新前端已显示内容 |
| `retrieval` | RAG 检索结果（full 模式） |
| `lifecycle` | turn_start、turn_end、turn_error、recursion_limit_reached 等 |
| `command_response` | 命令执行结果文本 |
| `session_reset` | 会话重置，含 `memory` 信息 |
| `session_compacted` | 手动压缩完成 |
| `done` | 本轮结束，含 `content`、`usage`、`session_id` |
| `error` | 错误信息 |

---

## 四、EventBus — 按 Agent 的发布订阅

**位置**：第 177-202 行

### 数据结构

```python
_queues: dict[str, list[asyncio.Queue]]
# 例如: {"main": [queue1, queue2], "coder": [queue3]}
```

- **key**：`agent_id`
- **value**：该 agent 下所有订阅者的队列列表（一个 agent 可有多个前端连接）

### API

| 方法 | 作用 |
|------|------|
| `subscribe(agent_id)` | 新建 `asyncio.Queue`，登记到 `_queues[agent_id]`，返回 queue；订阅者用 `await queue.get()` 等待事件 |
| `unsubscribe(agent_id, queue)` | 从 `_queues[agent_id]` 移除该 queue，连接断开时调用 |
| `emit(agent_id, event)` | 遍历 `_queues[agent_id]` 中所有 queue，对每个执行 `put_nowait(event)` |

### 数据流

```
前端 GET /api/agents/{agent_id}/events
        │
        ▼
queue = event_bus.subscribe(agent_id)
        │
        ▼
while True: event = await queue.get() → yield SSE
        │
        ▼
(连接断开) event_bus.unsubscribe(agent_id, queue)

发送端（agent、tools、heartbeat 等）：
event_bus.emit(agent_id, {"type": "...", "event": "..."})
        │
        ▼
遍历 _queues[agent_id]，put_nowait(event) → 订阅者 queue.get() 收到
```

### 事件类型示例

| 事件 | 来源 | 含义 |
|------|------|------|
| `tool_dangerous_executed` | agent.py | 危险工具（exec、process_kill）执行后 |
| `auto_compact_start` / `auto_compact_done` | agent.py | 自动压缩开始/结束 |
| `session_memory_saved` / `session_memory_failed` | agent.py | session-memory 保存结果 |
| `subagent_start` / `subagent_progress` / `subagent_done` | agent_tools.py | 子 Agent 启动/进度/完成 |
| `subagent_announce` | agent_tools.py | 子 Agent 结果投递状态 |
| `skills_updated` | skills_watcher.py | Skills 更新 |

### 与 Chat SSE 的区别

| 维度 | EventBus | Chat API SSE |
|------|----------|--------------|
| 端点 | `GET /api/agents/{id}/events` | `POST /api/chat` (stream=True) |
| 内容 | 生命周期事件（压缩、子 Agent、工具等） | 对话 token 流 |
| 触发 | 后台事件驱动 | 用户发消息驱动 |

---

## 五、相关概念补充

### 消息队列 vs 会话锁

| 维度 | 消息队列 (message_queue.py) | 会话锁 (session_manager._locks) |
|------|----------------------------|--------------------------------|
| 层级 | 应用层（请求调度） | I/O 层（磁盘写） |
| 保护 | 同一会话只跑一个 turn | 同一会话文件只被一个写操作修改 |
| 使用 | chat API 流式路径 | `_save_session_data` 内 |
| 主/子 | 主会话通过 chat API 时用；子 Agent 执行不经过队列 | 主、子会话写入时都会用 |

### 队列与锁的创建时机

- **队列**：按需创建，`get_queue(agent_id, session_id)` 首次调用时创建；子 Agent 执行不经过 chat API，故不创建队列
- **锁**：写入时 `_get_lock(session_id, agent_id)` 按需创建；主、子会话写入都会上锁

### 生命周期钩子

- `LifecycleHooks` 已定义（`on_before_tool_call`、`on_after_tool_call`），`astream` 中有调用点
- `agent_manager.lifecycle_hooks` 始终为 `None`，未接入

---

## 六、下一步

进入 **模块四：Prompt 构建 `prompt_builder.py`** —— 系统提示如何组装。

---

# 模块四：Prompt 构建 `prompt_builder.py`

> 学习路径：系统提示如何组装

## 一、整体定位

`prompt_builder` 负责**组装系统提示词**：把身份、工具、记忆、安全、心跳等规则拼成一段 system prompt，供 LLM 使用。支持三种模式，可选生成 PromptReport 做统计。

---

## 二、三种模式

| 模式 | 用途 | 包含内容 |
|------|------|----------|
| **full** | 主会话正常对话 | 身份、工具、消息路由、文档、安全、技能、记忆召回/写入、会话启动、时间、工作区、静默回复、心跳、运行时、项目上下文（含 MEMORY、每日笔记） |
| **minimal** | 子 Agent、announce、心跳 | 身份、工具、文档、安全、心跳、工作区、运行时；项目上下文只含 SOUL/IDENTITY/USER/AGENTS/TOOLS |
| **none** | 极简 | 仅一句身份描述 |

---

## 三、核心结构

- **PromptParams**：`agent_id`、`mode`、`available_tools`、`extra_system_prompt`、`max_file_chars`、`max_total_chars`、`locale`、`heartbeat_prompt`
- **PromptReport**：`mode`、`total_chars`、`sections`、`injected_files`、`tool_count`、`truncation_events`、`summary()`
- **项目上下文**：SOUL/IDENTITY/USER/AGENTS/TOOLS 等；full 模式含 HEARTBEAT、MEMORY、每日笔记；`_smart_truncate` 单文件前 70%+后 20%

---

## 四、专用 Prompt 方法

| 方法 | 用途 |
|------|------|
| `build_post_compaction_context` | 压缩后上下文刷新 |
| `build_memory_flush_prompt` / `build_memory_flush_system` | 压缩前记忆刷新 |
| `build_session_memory_prompt` | /new 时会话摘要 → memory |
| `build_bootstrap_prompt` | BOOTSTRAP.md 首次运行引导 |

---

## 五、announce 与 prompt_mode

- **announce 消息**：固定格式（`[系统消息] [会话ID: xxx] 子任务完成...`），由 `_build_announce_message` 生成
- **prompt_mode="minimal"**：控制的是 **system prompt**（指令），不是消息内容
- 原因：announce 只需处理通知，用 minimal 省 token、加快、任务匹配

---

## 六、下一步

进入 **模块五：工具系统 `tools/`** —— 工具如何注册、执行、与 Agent 协作。

---

# 模块五：工具系统 `tools/`

> 学习路径：工具如何注册、执行、与 Agent 协作

## 一、整体定位

工具系统为 Agent 提供可调用能力：文件、执行、网络、记忆、知识库、子 Agent、定时任务、状态等。工具基于 LangChain `BaseTool`，由 AgentManager 的 `_build_tools` 聚合，再经 `_filter_tools_by_policy` 按配置过滤后传给 ReAct Agent。

---

## 二、工具模块与聚合

| 模块 | 工具 | 说明 |
|------|------|------|
| file_tools | read, write, edit, grep, find, ls, apply_patch | 工作区内文件操作，路径校验；apply_patch 可选 |
| exec_tools | exec, python_repl, process_list, process_kill | 沙箱执行；exec/process_kill 支持 approval 确认 |
| web_tools | web_search, web_fetch | 网络搜索、网页抓取 |
| memory_tools | memory_search, memory_get | MEMORY.md + memory/*.md 检索 |
| knowledge_tool | search_knowledge_base | knowledge/ 目录语义检索 |
| agent_tools | agents_list, sessions_list, sessions_history, sessions_send, sessions_spawn, subagents | 会话与子 Agent 管理 |
| cron_tools | cron | 定时任务 list/add/update/remove/run/wake |
| status_tool | session_status | 会话状态卡片 |

---

## 三、策略过滤

`_filter_tools_by_policy` 按 `agents.list[].tools.allow/deny` 过滤：deny 优先；allow 为空表示全允许；`apply_patch` 视为 `exec` 的别名。

---

## 四、工具调用

- **LangGraph 调用**：至少实现 `_run`；`_arun` 可选（需异步等待如 approval 时实现）
- **Fallback 解析**：`tool_call_parser.parse_text_tool_calls` 解析 Kimi K2 等模型的文本形式工具调用

---

## 五、下一步

进入 **模块六：子 Agent 系统** —— 子 Agent 如何创建、执行、announce 投递。

---

# 模块六：子 Agent 系统

> 学习路径：子 Agent 如何创建、执行、announce 投递

## 一、整体定位

子 Agent 系统负责创建、执行、追踪和结果投递。父 Agent 通过 `sessions_spawn` 创建子 Agent，子 Agent 在独立会话中运行，完成后通过 announce 把结果投递给父会话。涉及 `agent_tools.py`、`subagent_registry.py`、`subagent_resume.py`。

---

## 二、核心流程

- **创建**：`sessions_spawn` → `registry.register_run` → `asyncio.run_coroutine_threadsafe(_run_subagent)`，子 Agent 在后台并行执行
- **执行**：`_run_subagent` 调用 `agent_manager.astream`，收集 token、tool 事件，完成后 `_deliver_announce_to_requester`
- **Announce 投递**：若 requester 忙（`is_session_busy`）则重试或丢弃；不忙则 `astream(announce_msg, persist_input_role="system")` 触发新 turn

---

## 三、子子 Agent 与递归投递

- **子 Agent 处理子子结果**：子子完成 → announce 投递给子 → 子 Agent 跑一轮 LLM，用 `parent_reply` 作为 result 再投递给祖父；**不是嵌套**，是**总结后提交**
- **子 Agent 不等待**：spawn 后立即返回，子 Agent 可继续做其他事；子子结果在**后续单独 turn** 处理

---

## 四、队列与「忙」

- **队列**：按 `agent_id:session_id`，每个 **session** 一个队列
- **忙**：`is_session_busy` 看该 session 的队列是否被 acquire
- **acquire 时机**：用户**发送消息**时（不是打字时）；chat API 从请求开始到 astream 结束持有
- **子 Agent、announce**：不经过 chat API，不 acquire 队列

---

## 五、多轮回复

- 主 Agent 第一次回复用户后，子 Agent 完成 → announce 投递 → 主会话**新 turn**，LLM 收到 announce 再回复一次
- 用户会看到：第一条（主 Agent 对用户）、第二条（主 Agent 对子 Agent 结果）

---

## 六、多子 Agent 并行

- 主 Agent 可 spawn 多个子 Agent 并行执行
- **每个子 Agent 完成**都会**单独触发**主会话 turn，不是等全部完成再统一回复
- 前端可能看到多条依次的 assistant 消息，顺序取决于完成先后

---

## 七、下一步

进入 **模块七：Heartbeat 与 Cron** —— 心跳轮询、定时任务。

---

# 模块七：Heartbeat 与 Cron

> 学习路径：心跳轮询主会话、定时任务调度、与 System Events 配合

## 一、整体定位

- **Heartbeat**：按 `interval_seconds` 周期性轮询主会话，检查 HEARTBEAT.md 或 pending cron 事件，调用 LLM 后决定是否推送
- **Cron**：定时任务调度，到点触发 `enqueue_system_event` + `request_heartbeat_now`，由心跳读取并投递
- **仅主会话**：子会话（subagent）无心跳、无定时任务

---

## 二、核心数据流

```
Cron 到点 → enqueue_system_event(session_key, context_key:"cron:{job_id}")
         → request_heartbeat_now(agent_id) 唤醒心跳
         → 心跳 loop 检测 _wake_requested，跳出 sleep 立即执行
         → peek_system_event_entries_for_agent() 取 pending
         → 若有 cron 事件用 _build_cron_event_prompt，否则读 HEARTBEAT.md
         → agent_manager.astream(prompt_mode="minimal")
         → strip_heartbeat_token：HEARTBEAT_OK 则 rollback 不持久化
         → 有实质内容则 drain 消费事件，event_bus.emit("heartbeat_message") 推送
```

---

## 三、Heartbeat 要点

- **HeartbeatRunner**：每 agent 一个 `_heartbeat_loop` 任务，`request_heartbeat_now` 将 agent_id 加入 `_wake_requested` 可提前唤醒
- **跳过条件**：静默时段（activeHours）、会话忙（is_session_busy）、HEARTBEAT.md 空
- **HEARTBEAT_OK**：纯 ack 时 rollback_last_turn，不写入历史；cron 事件未送达则不消费，下次再试
- **事件状态**：ok-empty | ok-token | sent | skipped | failed

---

## 四、Cron 要点

- **调度类型**：`at`（绝对时间一次）、`every`（间隔 ms）、`cron`（表达式）
- **CronScheduler**：主循环 load_cron_store → 找 due jobs → _fire_job（enqueue + request_heartbeat_now）→ 重算 next_run → save
- **持久化**：`data/cron/jobs.json`，重启后加载
- **cron 工具**：list/add/update/remove/run/wake；wake 不入 store，直接 enqueue + 唤醒

---

## 五、心跳的主要作用（除配合 Cron 外）

- **轮询 HEARTBEAT.md**：按检查清单执行，可静默完成（HEARTBEAT_OK）或通知用户
- **后台维护**：记忆整理、工作区整理、文档更新（见 AGENTS.md）
- **静默时段**：activeHours 外直接 skipped

---

## 六、当前调度架构的问题与统一调度器设计

### 当前架构：分散的独立状态机

系统中有多种事件各自独立管理，靠约定配合争抢主 session 的写入权：

**显式持锁（`queue.acquire()`）：**
- 用户消息（`api/chat.py`）
- 子 Agent announce 到主 session（`agent_tools.py`）
- followup 队列（`api/chat.py`，用户消息处理完后自动排空）

**软协调（检查 `is_session_busy`，忙则跳过，不持锁）：**
- Heartbeat（`_run_heartbeat`）
- 子 Agent announce 到子 session（`agent_tools.py`）

**完全不管锁（潜在 bug）：**
- 重启恢复 announce（`subagent_resume.py`）

**常驻主事件循环、不写 session 的协程（独立生命周期）：**
- `cron._loop`、`heartbeat._heartbeat_loop`（定时器角色）
- `_periodic_state_save`（AgentState 持久化）
- `_incremental_ingest` / `_batch_ingest_messages`（记忆入库，fire-and-forget）
- `_run_subagent`（子 session 独立执行，不共享主 session）

**核心问题：cron 隐式依赖 heartbeat**。cron 触发后只是往 `system_events` 内存队列写入，由 heartbeat 在 session 空闲时读取执行。如果 heartbeat 关闭，cron 事件静默丢失，没有任何报错。且 `system_events` 是纯内存结构，服务重启后未消费事件全部丢失。

---

### 统一调度器设计

**核心原则**：队列锁保护的是"往 session 写入"这一条路，与 session 无关的协程（ingest、state_save、subagent 子 session 执行）不需要入队。

**统一任务描述：**

```python
@dataclass
class DispatchTask:
    session_key: str       # 目标 session
    priority: int          # 优先级
    payload: str           # 发给 Agent 的消息内容
    source: str            # 来源（"user"/"cron"/"announce"/"heartbeat"）
    on_success: Callable   # 成功后回调（各自处理自己的状态变更）
    on_failure: Callable   # 失败后回调
```

**优先级排列（数字越小越优先）：**

```
优先级 1 — 用户消息          （用户等待响应，最高优先级）
优先级 2 — 子 Agent announce  （结果及时送达父 Agent）
优先级 3 — 重启恢复 announce  （补发历史通知）
优先级 4 — cron 事件          （定时提醒）
优先级 5 — heartbeat          （主动巡检，最低优先级）
```

**调度器状态机（三个状态）：**
```
pending → executing → done（触发 on_success 或 on_failure 回调）
```
各事件的内部状态机（announce_state、heartbeat status、cron last_run_at）保持不变，由回调自行处理。

**与现有机制的关系：**
- `followup` 队列不再需要，followup 消息直接以同等优先级再入调度队列
- `system_events` 不再需要，cron 触发时直接入调度队列
- `heartbeat` 不再是 cron 的依赖，cron 入队后调度器独立消费
- 常驻协程（cron `_loop`、heartbeat `_loop`）角色变为"定时投递"，不再直接调 `astream()`

**实现选型：**
- 当前单进程架构：`asyncio.PriorityQueue` 足够，零运维，支持优先级，协程原生兼容
- 多进程/多机器横向扩展时：迁移到 RabbitMQ 等外部消息队列

**当前阶段最低成本改进（不上调度器时的兜底）：**
1. 启动时检测 cron 启用但 heartbeat 未启用 → 打 warning 日志
2. `system_events` 持久化到磁盘，服务重启后恢复未消费事件
